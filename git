# 1. Git 초기화
git init

# 2. 모든 파일 추가
git add .

# 3. 첫 커밋
git commit -m "Initial commit"

# 4. 원격 저장소 연결 (GitHub, GitLab 등에서 URL 복사)
git remote add origin https://github.com/CHOI-AHRIN/repository.git

# 5. 브랜치 이름 설정 (master가 아닐 경우 변경)
git branch -M master

# 6. 원격 저장소로 업로드
git push -u origin master # 로컬 브랜치를 원격 브랜치와 연결(upstream 설정)


---

# 현재 브랜치 이름 확인
git branch
git branch -r

# 브랜치 생성
git branch ahrin

# 새로운 저장소에 브랜치 푸시
git push -u origin ahrin

# 브랜치 삭제 
git branch -d ahrin